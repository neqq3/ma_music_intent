{
  "config": {
    "abort": {
      "single_instance_allowed": "Only a single MA Music Intent configuration is allowed."
    },
    "step": {
      "user": {
        "title": "Set up MA Music Intent",
        "description": "Add MA Music Intent to register its Home Assistant services."
      }
    },
    "options": {
      "step": {
        "init": {
          "title": "AI Agent",
          "description": "By default this integration inherits the Assist preferred conversation agent. Enable the override only if you want this integration to always use a specific agent."
        }
      },
      "data": {
        "use_custom_agent": "Override Assist default agent",
        "agent_id": "Dedicated conversation agent"
      },
      "error": {
        "required": "Select a conversation agent."
      }
    }
  }
}
